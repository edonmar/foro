{% extends 'layout.html.twig' %}

{% block title %}
    Mis datos
{% endblock %}

{% block content %}
    <h1>Modificar mis datos</h1>
    {{ form_start(formulario) }}
    {{ form_widget(formulario) }}
    <a href="{{ path('portada') }}" class="btn btn-info"><i class="fa fa-arrow-left"></i> Volver a la portada</a>
    <button type="submit" class="btn btn-success"><i class="fa fa-check"></i> Guardar cambios</button>
    <a href="{{ path('usuario_cambiar_clave') }}" class="btn btn-warning"><i class="fa fa-key"></i> Cambiar contraseña</a>
    <a href="{{ path('usuario_eliminar', {'id': usuario.id}) }}" class="btn btn-danger"><i class="fa fa-trash"></i> Eliminar usuario</a>
    {{ form_end(formulario) }}
{% endblock %}


